import org.code.theater.*;
import org.code.media.*;

/*
 * SceneTwo displays average top speed statistics for car brands.
 */

public class SceneTwo extends Scene {

  /** Instance Variables */
  private int[][] topSpeed;
  
  /*
   * Constructs SceneTwo using a 2D array of speeds.
   */
  public SceneTwo(int[][] topSpeed) {
    this.topSpeed = topSpeed;
  }

  /*
   * Draws the scene title and speed statistics
   */
 public void drawScene() {
    drawText("Top Speeds: ", 80, 40);
    pause(2);

    drawSpeedStats();
  }

  /*
   * Displays average speeds for each car brand
   */
  public void drawSpeedStats() {
    for (int row = 0; row < topSpeed.length; row++) {
      int average = getAverageSpeed(row);
      clear("white");

      if (row == 0) {
        drawText("Jeep Avg Speed: " + average + " MPH", 50, 200);
      } 
      else if (row == 1) {
        drawText("Lambo Avg Speed: " + average + " MPH", 50, 200);
      } 
      else {
        drawText("Ferrari Avg Speed: " + average + " MPH", 50, 200);
      }
      pause(2);
    }
  }

  /*
   * Calculates the average speed for a row
   */
  public int getAverageSpeed(int row) {
    int total = 0;
   // Sum all speeds in the row
    for (int col = 0; col < topSpeed[row].length; col++) {
      total += topSpeed[row][col];
    }

    return total / topSpeed[row].length;
  }
}
